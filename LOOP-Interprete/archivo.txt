clase Impar
	propiedades publicas:
		cadena hola


entero esImpar(entero numero)

si numero % 2 entonces
	devolver 1
sino numero % 2 entonces
	devolver 3
sino numero + numero entonces
	devolver 3
sino numero % 2 entonces
	devolver 3
sino
	devolver 3
	devolver 0


// las funcioens siempre deben de llevar un devovler 
// las funciones con algo adentro deben de llevar enter

clase HolaMundo
	propiedades privadas:
		entero a
		cadena nombre
		entero var
	metodos publicos:
constructor()
	a = d
	nombre = d
// en los construcotore no se puede asignar valores por lo que vi
// solo se pueden asignar a variables


entero inicio()

escribir " escriba su nombre "
// se guarda una cadena de texto en la variable nombre
leer nombre;
escribir " escriba un valor entre 1 y 30"
// se guarda un numero en la variable a
leer a
/* escribe en pantalla la constante junto
** al valor de la variable nombre
*/
escribir " Hola ", nombre
si numero % 2 entonces
	devolver 1
sino
	devolver 3

	devolver 0

// asi se coloca para meter ciclos

/* comentario */
/* area de declaracion de funcion de carga inicial */


entero Principal()
// comentario
HolaMundo hola = instanciar HolaMundo()
hola.inicio()
entero a[4] ={ 1, 3, 4 }
cadena b[]
b[0] = 1
b = instanciar entero[3]

escribir r1.obteenrARea(), re2.obtenerARe()






